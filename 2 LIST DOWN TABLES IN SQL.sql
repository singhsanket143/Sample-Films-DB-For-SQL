-- To perform any operation on a DB, we need to select the DB first
-- Because there might a lot of DBs in your machine, which DB do you target
-- to run the query on, is identified using the `USE db_name` command


USE LCO_FILMS;

-- How to get a list of all tables in a db ?

SHOW TABLES;
